该文件所对应的c文件是用来解析torrent文件的。

其中包含两个数据结构。一个是annlist，另一个是dowfile
annlist用于保存获取的tracker的url，这个的值是保存在torrent的announce或announce-list中
donfile用于保存将要下载文件的文件名，具体包括其路径及长度

包含15个函数。
readtorr获取torrent中的信息
findkey查找关键字
getann获取各个tracker的地址
addann向tracker添加一个url

getpilen获取piece的长度
getpihash读取各个piece的哈希值

ismulti判断下载的是单个文件还是多个文件
getfname获取文件名
getflen获取文件总长度
getflap获取各个文件的长度和路径


getpeid获取peer的id
getinhash获取info关键词对应的值计算hash

freemtor释放处理torrent分配的内存
rtorrent调用所以函数，以处理这个文件
